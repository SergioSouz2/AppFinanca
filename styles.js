import { StyleSheet } from 'react-native'
import { COLOR } from './src/theme/styles'


export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    card:{
        width:'100%',
        alignItems:'center',
    }
    
})